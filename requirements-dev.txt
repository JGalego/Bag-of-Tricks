# dev/CI tooling only — the tricks themselves need no third-party packages to
# run their offline path. The --llm modes need one provider SDK (anthropic /
# openai / google-genai), imported lazily; tests stub that seam, so CI needs
# none of them installed.
ruff==0.9.6
pytest>=8,<9
# .env loading: the --llm tricks call python-dotenv's load_dotenv (imported
# lazily; absent -> keys just come from the real env). Here so CI exercises it;
# the .env tests importorskip it so the suite still passes without it.
python-dotenv>=1.0
# logo generation (assets/generate.py): SVG->PNG and GIF assembly. Optional —
# only needed to regenerate the logos, not to run the tricks or tests.
cairosvg>=2.7
Pillow>=10
